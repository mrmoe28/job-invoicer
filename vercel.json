{
  "buildCommand": "pnpm turbo build --filter=@pulsecrm/web",
  "installCommand": "pnpm install --no-frozen-lockfile --config.optional=false",
  "outputDirectory": "apps/web/.next",
  "framework": null,
  "regions": ["iad1"],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
